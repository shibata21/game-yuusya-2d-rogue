# TODO

## Phaser一本化

- Godot / Pixi / 旧Canvas実行系は廃止済み。
- 正規実装は `index.html` + `src/` のPhaser/Vite版。
- 配布は `npm run build` で `dist/` へ生成する。

## 画像素材

- 48pxセルの高密度ドット素材を継続利用する。
- `assets/pixel/source/v6-self-made/` の8方向・アクション別個別PNGから `actors.png` / `tiles.png` / `effects.png` / `sprites.json` を再生成する。
- モンスター・勇者・タイル素材は外部素材を使わず、ローカル生成スクリプトの図形描画で自製する。
- 読み込みURLには `PIXEL_ASSET_VERSION` のバージョン文字列を付ける。
- Vite build時は `vite.config.mjs` で `assets/pixel/` を `dist/assets/pixel/` へコピーする。

## 実装方針

- ゲームルールは `src/gameCore.js` に集約し、Phaser非依存に保つ。
- Phaser側は `src/main.js` で描画・入力・HUD同期だけを担当する。
- `createGame({ seed })` から独立状態を作り、Vitestでは `update(ms)` を直接呼ぶ。
- 既存の数値バランス、種別名、鉱脈名、勇者職業、ウェーブ仕様は維持する。
- `localStorage` / `sessionStorage` は使わない。

## 未実装: 戦闘・移動改善

ラベル: `[バグ]` 既存挙動の不具合、`[仕様変更]` ルール変更、`[追加]` 新しい演出・機能、`[調査]` 原因特定込み、`[テスト]` 検証追加。

- `[調査]` `[バグ]` `pass_to_codex/S__262758406.jpg` のW51相当の状態を再現対象にする。勇者が入口付近で固まり、魔物が下層に滞留する問題を確認する。
- `[仕様変更]` 魔物の通常徘徊からホーム3マス制限を廃止し、接続済み通路内の到達可能マスへ平等に移動できるようにする。戦闘時の追跡・攻撃AIは維持する。
- `[仕様変更]` 攻撃射程内で勇者を攻撃可能な魔物は、他魔物との位置交換をしない。射程外や非戦闘時の詰まり回避交換は維持する。
- `[仕様変更]` 勇者は全員出現完了後、0.5秒の全員停止を挟んでから行動開始する。この待機中は勇者移動・勇者攻撃・魔物攻撃・魔物追跡を止める。
- `[仕様変更]` `[追加]` 僧侶と聖女を大幅強化する。HP増加、回復量・回復頻度強化、僧侶中心の範囲ヒール、範囲ヒールエフェクトを追加する。具体値は実装時にテストとプレイ感で固定する。
- `[仕様変更]` 卵は魔物と勇者が通過可能な床上オブジェクト扱いにする。卵同士は重ねない。孵化時に卵上が埋まっていれば近くの空き通路へ孵化し、近くも空いていなければ待機する。
- `[仕様変更]` 捕食は捕食側のHPが満タンなら発生しない。HPが減っている時だけ現行確率で捕食する。
- `[バグ]` コア隣接は掘削可能判定だけ通路扱いしない。コアに隣接しているだけの土は掘れず、初期の掘れるマークも出さない。通常通路に隣接した土は従来通り掘れる。
- `[調査]` `[バグ]` 勇者が飛び道具を受けた後、初期位置や入口付近で固まるように見える問題を原因特定して修正する。卵通過、入口密集、占有マス経路探索、経路なし時の再探索をまとめて確認する。
- `[バグ]` `[テスト]` W50付近で勇者が初期位置から動かない問題を再現テスト化し、入口から離脱してコア方面へ進めることを確認する。
- `[追加]` 勇者がコア攻撃中だと初見で分かるように、コア点滅、赤い衝撃波、コアHPバーの揺れ・点滅を追加する。

追加テスト候補:

- `[テスト]` ホームから遠い通路にも魔物が到達できる。
- `[テスト]` 攻撃射程内の魔物は交換しないが、非戦闘時は詰まり回避できる。
- `[テスト]` 全勇者出現後0.5秒は勇者・魔物とも攻撃・移動しない。
- `[テスト]` 僧侶・聖女が範囲内の複数勇者を回復し、回復エフェクトが出る。
- `[テスト]` 卵上を勇者・魔物が通過でき、孵化時に近くへずれて孵化する。
- `[テスト]` HP満タンの魔物は捕食しない。
- `[テスト]` コア隣接だけでは掘削可能・初期ヒントにならない。
- `[テスト]` W51相当の混雑状態で勇者が入口付近から動き出す。
- `[テスト]` コア攻撃時に盤面演出とHUD警告が発生する。

## 検証

正規検証:

```bash
npm run assets:check
npm test
npm run build
```

素材生成を変更した場合:

```bash
npm run assets:build
npm run assets:check
```

## 注意

- スクショ画像はGitに含めない。
- 修正後は関連ファイルだけをコミットする。
